syntax = "proto3";
package types;

option go_package = "github.com/sisu-network/sisu/x/sisu/types";

message ChainMetadata {
  string signer = 1;
  string chain = 2;

  // Used for solana
  string solanaConfirmBlock = 3;
}
