syntax = "proto3";
package types;

message GasPriceMsg {
  string chain = 1;
  int64 blockHeight = 2;
  int64 gasPrice = 3;
  string signer = 4;
}

message GasPriceRecord {
  string chain = 1;
  int64 blockHeight = 2;
  repeated GasPriceMsg messages = 3;
}
