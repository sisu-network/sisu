syntax = "proto3";
package types;

// Cosmos message to broadcast KeysignResult
message KeysignResult {
  string signer = 1;
  bool success = 2;
  string outChain = 3;
  string outHash = 4;
  bytes tx = 5;
  bytes signature = 6;
}
