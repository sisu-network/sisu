syntax = "proto3";
package types;

// Note: all new fields in this proto MUST be included in SerializeWithoutSigner in the go file.
message ObservedTx {
  string signer = 1;
  string chain = 2;
  int64 blockHeight = 3;
  string txHash = 4;
  bytes serialized = 5;
}
